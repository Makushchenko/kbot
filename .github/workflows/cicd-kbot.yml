name: CI/CD kbot
run-name: ${{ github.triggering_actor }} is triggered {{ github.workflow }}

on:
  push:
    branches:
      - develop

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run test
        run: make test